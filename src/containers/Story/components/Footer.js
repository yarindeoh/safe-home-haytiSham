import React from 'react';

export const Footer = () => {
    return (
        <div className="footer">
            <button>Share</button>
            <button>Support</button>
        </div>
    );
};
